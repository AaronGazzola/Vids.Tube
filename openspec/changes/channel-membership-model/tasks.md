## 1. Migration: schema + functions

- [x] 1.1 Create the migration via `npx supabase migration new channel_membership_model` (never manually). In it, alter `public.channels`: drop NOT NULL on `owner_user_id`; add `youtube_channel_id text` with a unique constraint, `merged_into_channel_id uuid references public.channels (id)`, and check constraint `channels_identity_check` requiring at least one of (`owner_user_id`, `youtube_channel_id`, `merged_into_channel_id`) non-null; add an index on `merged_into_channel_id`.
- [x] 1.2 Same migration: create `public.memberships` exactly as specified in design D2 (columns, `unique (channel_id, community_channel_id)`, `check (channel_id <> community_channel_id)`, FKs with `on delete cascade` from both channel columns) and `public.membership_stream_stats` (PK `(membership_id, stream_id)`, `membership_id` FK `on delete cascade`, `stream_id` FK `on delete cascade`, `xp int not null default 0`, `message_count int not null default 0`, `stream_started_at timestamptz`). Add index `memberships_community_idx on memberships (community_channel_id)`.
- [x] 1.3 Same migration: enable RLS on both new tables with public `select using (true)` policies and no client write policies (per spec `memberships` "Public read, service-role write").
- [x] 1.4 Same migration: create `public.level_for_xp(xp bigint) returns int` as an `immutable` SQL function returning `floor(sqrt(xp / 100.0))::int` (0 for xp < 100; thresholds 100/400/900 per spec scenario "Level thresholds").
- [x] 1.5 Same migration: create `public.recompute_membership(p_channel_id uuid, p_community_channel_id uuid) returns void` implementing design D3/D4 exactly: resolve the channel's identity keys (`owner_user_id::text`, `'youtube:' || youtube_channel_id`); aggregate attendance/messages from `chat_messages` (matching `user_id = owner` OR `origin='youtube' AND external_author_id = ycid`) joined to the community's `streams`; per-stream xp = `greatest(sum(viewer_scores.total_score), 0)` matched by `participant_key`; upsert the membership (create if history exists, preserve `credits`/`rewards`); delete+insert its `membership_stream_stats`; compute `current_streak`/`best_streak` over the community's ended streams ordered by `started_at`; set `level = level_for_xp(lifetime_xp)`.
- [x] 1.6 Same migration: create `public.merge_youtube_identity(p_user_id uuid) returns jsonb` implementing design D5 steps 1–9 verbatim (verified-link guard, survivor/source resolution, claimed-conflict abort, raw-event re-key on `chat_messages`/`score_events`/`featured_messages`, per-stream `viewer_scores` delete+rebuild from `score_events` + `featured_messages`, text `participant_key` rewrite in `command_events`/`tts_requests`/`ask_requests`/`clip_markers`, `banned_participants` earliest-wins collision rule, membership credits-sum/rewards-union then delete source memberships, recompute survivor in every affected community, tombstone + identity move). Return a jsonb summary (`{merged: bool, reason?: text, communities: int}`).
- [x] 1.7 Same migration: `revoke execute` on `recompute_membership` and `merge_youtube_identity` from `anon` and `authenticated`.
- [x] 1.8 Push with `npx supabase db push`, then regenerate `supabase/types.ts` via `npx supabase gen types typescript --project-id <project-ref> > supabase/types.ts`.

## 2. Wiring

- [x] 2.1 In `worker/lib/verify-links.ts`, after a successful `verified_at` update, call `supabaseAdmin.rpc("merge_youtube_identity", { p_user_id })` for each verified user; on error, `console.error` and continue the loop (never throw out of the batch), matching the youtube-handle-link delta spec.
- [x] 2.2 In `app/[channelSlug]/page.actions.ts` `getChannelBySlugAction`, select `merged_into_channel_id`; when set, fetch the survivor's `slug` and return a redirect marker; in the channel page component (`app/[channelSlug]/page.tsx` + its hook), perform the client-side redirect to the survivor's slug per the channels delta spec "Merged channels redirect to their survivor".

## 3. Backfill + verification script

- [x] 3.1 Create `scripts/backfill-memberships.ts` (service-role, same env/client pattern as `scripts/import-youtube-vods.ts`): run `merge_youtube_identity` for every `youtube_links` row with `verified_at` non-null, then `recompute_membership(channel, ownerCommunity)` for every channel whose owner `user_id` or linked `youtube_channel_id` appears in the owner community's `chat_messages`/`viewer_scores`; log a per-membership summary line and run it once.
- [x] 3.2 Create `scripts/verify-membership-merge.ts` (service-role, self-cleaning, pattern of `scripts/verify-youtube-link.ts`): seed a synthetic verified `youtube_links` row + synthetic streams/chat/score rows for a throwaway identity pair, run the merge, assert — pooled `viewer_scores` single-row rebuild, participant-key rewrite in one command table, credits sum on membership collision, idempotent second run (no diffs), contested-identity abort — then delete all seeded rows. Run it and fix until green.

## 4. Governance close-out

- [x] 4.1 Run `npm run build` and the existing e2e suite; fix any regressions from the `channels` type changes (`owner_user_id` now nullable in generated types — audit compile errors at all `channels` call sites).
- [x] 4.2 Create a Linear verification issue in the Az team / Vids.Tube project: "Verify: identity merge on a real link verification next live stream (pooled !me stats, no duplicate viewer_scores rows)" and link it to AZ-169. (Created AZ-195, related to AZ-169.)
